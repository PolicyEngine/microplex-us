meta:
  country: us
  model_year: 2024
  policyengine_model: policyengine-us

sources:
  cps_asec: { dataset: cps_asec_2025_calendar_2024, role: spine }
  puf: { dataset: puf_2024, role: donor }
  acs: { dataset: acs_2024, role: donor }
  sipp: { dataset: sipp_2023, role: donor }
  scf: { dataset: scf_2022, role: donor }

spine:
  base: cps_asec
  method: support_spine
  support: { seed: 20260529 }
  halves:
    - { name: cps_keep, keep: all }
    - { name: synthetic_puf, strip_to: [demographics] }

imputation:
  - onto: synthetic_puf
    from: puf
    vars:
      - employment_income
      - partnership_s_corp_income
      - social_security
      - taxable_pension_income
      - interest_deduction
      - tax_exempt_pension_income
      - long_term_capital_gains
      - unreimbursed_business_employee_expenses
      - pre_tax_contributions
      - taxable_ira_distributions
      - self_employment_income
      - w2_wages_from_qualified_business
      - unadjusted_basis_qualified_property
      - business_is_sstb
      - sstb_self_employment_income_before_lsr
      - sstb_self_employment_income
      - sstb_self_employment_income_would_be_qualified
      - sstb_w2_wages_from_qualified_business
      - sstb_unadjusted_basis_qualified_property
      - short_term_capital_gains
      - qualified_dividend_income
      - charitable_cash_donations
      - self_employed_pension_contribution_ald
      - unrecaptured_section_1250_gain
      - taxable_unemployment_compensation
      - taxable_interest_income
      - domestic_production_ald
      - self_employed_health_insurance_ald
      - rental_income
      - non_qualified_dividend_income
      - cdcc_relevant_expenses
      - tax_exempt_interest_income
      - salt_refund_income
      - foreign_tax_credit
      - estate_income
      - charitable_non_cash_donations
      - american_opportunity_credit
      - miscellaneous_income
      - alimony_expense
      - farm_income
      - partnership_se_income
      - alimony_income
      - health_savings_account_ald
      - non_sch_d_capital_gains
      - general_business_credit
      - energy_efficient_home_improvement_credit
      - traditional_ira_contributions
      - amt_foreign_tax_credit
      - excess_withheld_payroll_tax
      - savers_credit
      - student_loan_interest
      - investment_income_elected_form_4952
      - early_withdrawal_penalty
      - prior_year_minimum_tax_credit
      - farm_rent_income
      - qualified_tuition_expenses
      - educator_expense
      - long_term_capital_gains_on_collectibles
      - other_credits
      - casualty_loss
      - unreported_payroll_tax
      - recapture_of_investment_credit
      - deductible_mortgage_interest
      - home_mortgage_interest
      - investment_interest_expense
      - other_health_insurance_premiums
      - qualified_reit_and_ptp_income
      - qualified_bdc_income
      - farm_operations_income
      - estate_income_would_be_qualified
      - farm_operations_income_would_be_qualified
      - farm_rent_income_would_be_qualified
      - partnership_s_corp_income_would_be_qualified
      - rental_income_would_be_qualified
      - self_employment_income_would_be_qualified
      - weeks_unemployed
    condition_on: [demographics]
    order: spine_first
    synthesize: true

  - onto: cps_keep
    from: puf
    vars:
      - employment_income
      - partnership_s_corp_income
      - social_security
      - taxable_pension_income
      - interest_deduction
      - tax_exempt_pension_income
      - long_term_capital_gains
      - unreimbursed_business_employee_expenses
      - pre_tax_contributions
      - taxable_ira_distributions
      - self_employment_income
      - w2_wages_from_qualified_business
      - unadjusted_basis_qualified_property
      - business_is_sstb
      - sstb_self_employment_income_before_lsr
      - sstb_self_employment_income
      - sstb_self_employment_income_would_be_qualified
      - sstb_w2_wages_from_qualified_business
      - sstb_unadjusted_basis_qualified_property
      - short_term_capital_gains
      - qualified_dividend_income
      - charitable_cash_donations
      - self_employed_pension_contribution_ald
      - unrecaptured_section_1250_gain
      - taxable_unemployment_compensation
      - taxable_interest_income
      - domestic_production_ald
      - self_employed_health_insurance_ald
      - rental_income
      - non_qualified_dividend_income
      - cdcc_relevant_expenses
      - tax_exempt_interest_income
      - salt_refund_income
      - foreign_tax_credit
      - estate_income
      - charitable_non_cash_donations
      - american_opportunity_credit
      - miscellaneous_income
      - alimony_expense
      - farm_income
      - partnership_se_income
      - alimony_income
      - health_savings_account_ald
      - non_sch_d_capital_gains
      - general_business_credit
      - energy_efficient_home_improvement_credit
      - traditional_ira_contributions
      - amt_foreign_tax_credit
      - excess_withheld_payroll_tax
      - savers_credit
      - student_loan_interest
      - investment_income_elected_form_4952
      - early_withdrawal_penalty
      - prior_year_minimum_tax_credit
      - farm_rent_income
      - qualified_tuition_expenses
      - educator_expense
      - long_term_capital_gains_on_collectibles
      - other_credits
      - casualty_loss
      - unreported_payroll_tax
      - recapture_of_investment_credit
      - deductible_mortgage_interest
      - home_mortgage_interest
      - investment_interest_expense
      - other_health_insurance_premiums
      - qualified_reit_and_ptp_income
      - qualified_bdc_income
      - farm_operations_income
      - estate_income_would_be_qualified
      - farm_operations_income_would_be_qualified
      - farm_rent_income_would_be_qualified
      - partnership_s_corp_income_would_be_qualified
      - rental_income_would_be_qualified
      - self_employment_income_would_be_qualified
      - weeks_unemployed
    condition_on: [demographics]
    order: spine_first

  - onto: cps_keep
    from: puf
    vars:
      - partnership_s_corp_income
      - interest_deduction
      - unreimbursed_business_employee_expenses
      - pre_tax_contributions
      - w2_wages_from_qualified_business
      - unadjusted_basis_qualified_property
      - business_is_sstb
      - sstb_self_employment_income_before_lsr
      - sstb_self_employment_income
      - sstb_self_employment_income_would_be_qualified
      - sstb_w2_wages_from_qualified_business
      - sstb_unadjusted_basis_qualified_property
      - charitable_cash_donations
      - self_employed_pension_contribution_ald
      - unrecaptured_section_1250_gain
      - taxable_unemployment_compensation
      - domestic_production_ald
      - self_employed_health_insurance_ald
      - cdcc_relevant_expenses
      - salt_refund_income
      - foreign_tax_credit
      - estate_income
      - charitable_non_cash_donations
      - american_opportunity_credit
      - miscellaneous_income
      - alimony_expense
      - health_savings_account_ald
      - non_sch_d_capital_gains
      - general_business_credit
      - energy_efficient_home_improvement_credit
      - amt_foreign_tax_credit
      - excess_withheld_payroll_tax
      - savers_credit
      - student_loan_interest
      - investment_income_elected_form_4952
      - early_withdrawal_penalty
      - prior_year_minimum_tax_credit
      - farm_rent_income
      - qualified_tuition_expenses
      - educator_expense
      - long_term_capital_gains_on_collectibles
      - other_credits
      - casualty_loss
      - unreported_payroll_tax
      - recapture_of_investment_credit
      - deductible_mortgage_interest
      - home_mortgage_interest
      - investment_interest_expense
      - other_health_insurance_premiums
      - qualified_reit_and_ptp_income
      - qualified_bdc_income
      - farm_operations_income
      - estate_income_would_be_qualified
      - farm_operations_income_would_be_qualified
      - farm_rent_income_would_be_qualified
      - partnership_s_corp_income_would_be_qualified
      - rental_income_would_be_qualified
      - self_employment_income_would_be_qualified
    condition_on: [demographics]
    order: spine_first
    synthesize: true

variables:
  age:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  alimony_expense:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  alimony_income:
    entity: person
    role: puf_imputed
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
      notes: MP legacy keeps CPS values for same-name non-overridden columns; this is explicitly called out for known collisions.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: 'Known keep-CPS collision: the synthetic half must synthesize this variable and must not inherit the CPS canonical
        value. Known keep-CPS collision from Claude manifest; synthetic half must not keep CPS canonical value.'
  american_opportunity_credit_claimed_prior_years:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  attends_eligible_educational_institution_for_american_opportunity_credit:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  auto_loan_balance:
    entity: household
    role: scf_direct_or_support
    temporary: true
    ecps:
      method: SCF donor imputation support variable
      code:
      - path: policyengine_us_data/calibration/source_impute.py
        lines: 668-814
        symbol: impute_scf_variables
    mp_legacy:
      method: SCF support variable with export default fallback
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 8398-8400
        summary: Legacy rebuild requests net_worth/auto-loan SCF support variables.
      - path: src/microplex_us/policyengine/us.py
        lines: 506,521
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
    mp_spec:
      method: source=scf donor imputation/support variable
      operation:
        kind: impute
        source: scf
        imputation_step: scf_source_impute
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary variable provenance; concrete source-imputation step still pending.
  auto_loan_interest:
    entity: household
    role: scf_direct_or_support
    temporary: true
    ecps:
      method: SCF donor imputation support variable
      code:
      - path: policyengine_us_data/calibration/source_impute.py
        lines: 668-814
        symbol: impute_scf_variables
    mp_legacy:
      method: SCF support variable with export default fallback
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 8398-8400
        summary: Legacy rebuild requests net_worth/auto-loan SCF support variables.
      - path: src/microplex_us/policyengine/us.py
        lines: 506,521
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
    mp_spec:
      method: source=scf donor imputation/support variable
      operation:
        kind: impute
        source: scf
        imputation_step: scf_source_impute
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary variable provenance; concrete source-imputation step still pending.
  bank_account_assets:
    entity: person
    role: sipp_asset_source_imputed
    temporary: true
    ecps:
      method: SIPP asset donor QRF imputation
      code:
      - path: policyengine_us_data/calibration/source_impute.py
        lines: 37-42,562-657
        symbol: SIPP_ASSETS_VARIABLES / impute_sipp_assets
    mp_legacy:
      method: SIPP donor survey/source-impute target
      code:
      - path: src/microplex_us/pe_source_impute_specs.py
        summary: MP legacy PE-source donor block specs.
      - path: src/microplex_us/data_sources/donor_surveys.py
        lines: 1236-1291
        symbol: SIPPSourceProvider / SIPPAssetsSourceProvider
    mp_spec:
      method: source=sipp asset donor imputation
      operation:
        kind: impute
        source: sipp
        imputation_step: sipp_source_impute
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration for future SourceRegistry-backed source-imputation step.
  block_geoid:
    entity: household
    role: geography_export_contract
    temporary: true
    ecps:
      method: eCPS geography assignment/export encoding
      code:
      - path: policyengine_us_data/storage/enhanced_cps/clone_and_assign.py
        lines: 55,114-151
        symbol: assign_random_geography
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 269-289
        summary: Fixed-width block/tract/county export encoding.
      notes: eCPS emits fixed-width byte strings for block/tract/county geographies.
    mp_legacy:
      method: Microplex census block assignment/export derivation
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 491-587,3999-4023
        symbol: _attach_household_census_geographies / _ensure_household_in_nyc
      - path: src/microplex_us/geography.py
        lines: 403-527,550-647
        symbol: BlockGeography
      notes: Manifest flagged possible encoding/export divergence; exporter must preserve eCPS-compatible widths.
    mp_spec:
      method: geography assignment/exporter derived geography with eCPS-compatible encoding
      operation:
        kind: encode_geoid
        source: geography
        encoding: S15
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration for future geography assignment/exporter rules.
  bond_assets:
    entity: person
    role: sipp_asset_source_imputed
    temporary: true
    ecps:
      method: SIPP asset donor QRF imputation
      code:
      - path: policyengine_us_data/calibration/source_impute.py
        lines: 37-42,562-657
        symbol: SIPP_ASSETS_VARIABLES / impute_sipp_assets
    mp_legacy:
      method: SIPP donor survey/source-impute target
      code:
      - path: src/microplex_us/pe_source_impute_specs.py
        summary: MP legacy PE-source donor block specs.
      - path: src/microplex_us/data_sources/donor_surveys.py
        lines: 1236-1291
        symbol: SIPPSourceProvider / SIPPAssetsSourceProvider
    mp_spec:
      method: source=sipp asset donor imputation
      operation:
        kind: impute
        source: sipp
        imputation_step: sipp_source_impute
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration for future SourceRegistry-backed source-imputation step.
  business_is_sstb:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  casualty_loss:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  charitable_cash_donations:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  charitable_non_cash_donations:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  child_support_expense:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  child_support_received:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  congressional_district_geoid:
    entity: household
    role: geography_export_contract
    temporary: true
    ecps:
      method: eCPS geography assignment/export encoding
      code:
      - path: policyengine_us_data/storage/enhanced_cps/clone_and_assign.py
        lines: 55,114-151
        symbol: assign_random_geography
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 269-289
        summary: Fixed-width block/tract/county export encoding.
      notes: eCPS emits fixed-width byte strings for block/tract/county geographies.
    mp_legacy:
      method: Microplex census block assignment/export derivation
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 491-587,3999-4023
        symbol: _attach_household_census_geographies / _ensure_household_in_nyc
      - path: src/microplex_us/geography.py
        lines: 403-527,550-647
        symbol: BlockGeography
      notes: Manifest flagged possible encoding/export divergence; exporter must preserve eCPS-compatible widths.
    mp_spec:
      method: geography assignment/exporter derived geography with eCPS-compatible encoding
      operation:
        kind: encode_geoid
        source: geography
        encoding: S4
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration for future geography assignment/exporter rules.
  count_under_18:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  count_under_6:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  county_fips:
    entity: household
    role: geography_export_contract
    temporary: true
    ecps:
      method: eCPS geography assignment/export encoding
      code:
      - path: policyengine_us_data/storage/enhanced_cps/clone_and_assign.py
        lines: 55,114-151
        symbol: assign_random_geography
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 269-289
        summary: Fixed-width block/tract/county export encoding.
      notes: eCPS emits fixed-width byte strings for block/tract/county geographies.
    mp_legacy:
      method: Microplex census block assignment/export derivation
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 491-587,3999-4023
        symbol: _attach_household_census_geographies / _ensure_household_in_nyc
      - path: src/microplex_us/geography.py
        lines: 403-527,550-647
        symbol: BlockGeography
      notes: Manifest flagged possible encoding/export divergence; exporter must preserve eCPS-compatible widths.
    mp_spec:
      method: geography assignment/exporter derived geography with eCPS-compatible encoding
      operation:
        kind: encode_geoid
        source: geography
        encoding: S5
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration for future geography assignment/exporter rules.
  cps_race:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  detailed_occupation_recode:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  difficulty_doing_errands:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  difficulty_dressing_or_bathing:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  difficulty_hearing:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  difficulty_remembering_or_making_decisions:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  difficulty_seeing:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  difficulty_walking_or_climbing_stairs:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  disability_benefits:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  domestic_production_ald:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  educational_assistance:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  educator_expense:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  employer_sponsored_insurance_premiums:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  employment_income_before_lsr:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  estate_income:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  estate_income_would_be_qualified:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  family_id:
    entity: family
    role: structural_export_contract
    temporary: true
    ecps:
      method: PolicyEngine H5 structural id/link/weight export
      code:
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: eCPS structural entity ids, links, and household_weight export.
    mp_legacy:
      method: PolicyEngine US H5 structural id/link/weight export
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 473-486
        symbol: POLICYENGINE_US_STRUCTURAL_EXPORT_COLUMNS
    mp_spec:
      method: exporter structural entity/link column
      operation:
        kind: structural_export
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Structural columns must be asserted by strict stage/export manifests.
  farm_income:
    entity: person
    role: puf_imputed
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF; CPS keep half may pass through unless explicitly overridden.
  farm_operations_income:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  farm_operations_income_would_be_qualified:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  farm_rent_income:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  farm_rent_income_would_be_qualified:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  financial_assistance:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  first_home_mortgage_balance:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  first_home_mortgage_interest:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  first_home_mortgage_origination_year:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  fsla_overtime_premium:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  has_american_opportunity_credit_1098_t_or_exception:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  has_american_opportunity_credit_institution_ein:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  has_champva_health_coverage_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: has_champva_health_coverage_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  has_completed_first_four_years_of_postsecondary_education:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  has_esi:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: has_esi
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  has_felony_drug_conviction:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  has_indian_health_service_coverage_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: has_indian_health_service_coverage_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  has_itin:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  has_marketplace_health_coverage:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: has_marketplace_health_coverage
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  has_marketplace_health_coverage_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: has_marketplace_health_coverage_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  has_medicaid_health_coverage_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: has_medicaid_health_coverage_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  has_never_worked:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  has_non_marketplace_direct_purchase_health_coverage_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: has_non_marketplace_direct_purchase_health_coverage_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  has_other_means_tested_health_coverage_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: has_other_means_tested_health_coverage_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  has_tin:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  has_tricare_health_coverage_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: has_tricare_health_coverage_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  has_va_health_coverage_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: has_va_health_coverage_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  has_valid_ssn:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  health_insurance_premiums_without_medicare_part_b:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  health_savings_account_ald:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  home_mortgage_interest:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: 'Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables. Current
        spec inherited this from the legacy PUF surface; verify whether eCPS treats it as formula/default before promoting.
        Needs review: current MP spec inherited this from legacy PUF surface, but the manifest questions eCPS formula/default
        treatment.'
  hourly_wage:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  hours_worked_last_week:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  household_id:
    entity: household
    role: structural_export_contract
    temporary: true
    ecps:
      method: PolicyEngine H5 structural id/link/weight export
      code:
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: eCPS structural entity ids, links, and household_weight export.
    mp_legacy:
      method: PolicyEngine US H5 structural id/link/weight export
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 473-486
        symbol: POLICYENGINE_US_STRUCTURAL_EXPORT_COLUMNS
    mp_spec:
      method: exporter structural entity/link column
      operation:
        kind: structural_export
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Structural columns must be asserted by strict stage/export manifests.
  household_vehicles_owned:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  household_vehicles_value:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  household_weight:
    entity: household
    role: structural_export_contract
    temporary: true
    ecps:
      method: PolicyEngine H5 structural id/link/weight export
      code:
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: eCPS structural entity ids, links, and household_weight export.
    mp_legacy:
      method: PolicyEngine US H5 structural id/link/weight export
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 473-486
        symbol: POLICYENGINE_US_STRUCTURAL_EXPORT_COLUMNS
    mp_spec:
      method: exporter structural entity/link column
      operation:
        kind: structural_export
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Structural columns must be asserted by strict stage/export manifests.
  immigration_status_str:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  in_nyc:
    entity: household
    role: geography_export_contract
    temporary: true
    ecps:
      method: eCPS geography assignment/export encoding
      code:
      - path: policyengine_us_data/storage/enhanced_cps/clone_and_assign.py
        lines: 55,114-151
        symbol: assign_random_geography
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 269-289
        summary: Fixed-width block/tract/county export encoding.
      notes: eCPS emits fixed-width byte strings for block/tract/county geographies.
    mp_legacy:
      method: Microplex census block assignment/export derivation
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 491-587,3999-4023
        symbol: _attach_household_census_geographies / _ensure_household_in_nyc
      - path: src/microplex_us/geography.py
        lines: 403-527,550-647
        symbol: BlockGeography
      notes: Manifest flagged possible encoding/export divergence; exporter must preserve eCPS-compatible widths.
    mp_spec:
      method: geography assignment/exporter derived geography with eCPS-compatible encoding
      operation:
        kind: derive
        transform: geography_in_nyc
        depends_on: [county_fips]
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration for future geography assignment/exporter rules.
  investment_income_elected_form_4952:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  investment_interest_expense:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: 'Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables. Current
        spec inherited this from the legacy PUF surface; verify whether eCPS treats it as formula/default before promoting.
        Needs review: current MP spec inherited this from legacy PUF surface, but the manifest questions eCPS formula/default
        treatment.'
  is_blind:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  is_computer_scientist:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  is_disabled:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  is_enrolled_at_least_half_time_for_american_opportunity_credit:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  is_executive_administrative_professional:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  is_farmer_fisher:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  is_female:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  is_full_time_college_student:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  is_hispanic:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  is_household_head:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  is_military:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  is_paid_hourly:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  is_pregnant:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  is_pursuing_credential_for_american_opportunity_credit:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  is_separated:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  is_surviving_spouse:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  is_tipped_occupation:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  is_union_member_or_covered:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  is_unmarried_partner_of_household_head:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  is_wic_at_nutritional_risk:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  keogh_distributions:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  long_term_capital_gains_before_response:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  long_term_capital_gains_on_collectibles:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  marital_unit_id:
    entity: person
    role: structural_export_contract
    temporary: true
    ecps:
      method: PolicyEngine H5 structural id/link/weight export
      code:
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: eCPS structural entity ids, links, and household_weight export.
    mp_legacy:
      method: PolicyEngine US H5 structural id/link/weight export
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 473-486
        symbol: POLICYENGINE_US_STRUCTURAL_EXPORT_COLUMNS
    mp_spec:
      method: exporter structural entity/link column
      operation:
        kind: structural_export
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Structural columns must be asserted by strict stage/export manifests.
  meets_ssi_disability_criteria:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  miscellaneous_income:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  net_worth:
    entity: household
    role: net_worth_open_decision
    temporary: true
    ecps:
      method: direct SCF QRF imputation from SCF networth
      code:
      - path: policyengine_us_data/calibration/source_impute.py
        lines: 44-48,668-814,701-702
        symbol: NET_WORTH_VARIABLES / impute_scf_variables
      notes: Incumbent eCPS imputes net_worth directly, not as a component sum.
    mp_legacy:
      method: direct SCF net_worth support with export default fallback
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 8398-8400
        summary: Legacy rebuild requests net_worth with SCF support variables.
      - path: src/microplex_us/policyengine/us.py
        lines: 437,593
        summary: Net worth is in the export set and has a legacy default fallback.
      notes: Claude handoff found both incumbents direct-impute net_worth; component derivation is new work.
    mp_spec:
      method: 'OPEN: direct SCF impute now, component-derived later only after component panel is complete'
      operation:
        kind: open_decision
        notes: Direct SCF impute now unless Max chooses component-derived net worth.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary variable provenance records Max/Claude decision point.
      notes: Recommended low-risk path is direct SCF impute for replication; component-derived net_worth needs a complete
        SCF asset/liability panel and separate validation.
  non_qualified_dividend_income:
    entity: person
    role: puf_imputed
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF; CPS keep half may pass through unless explicitly overridden.
  non_sch_d_capital_gains:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  other_health_insurance_premiums:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: 'Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables. Current
        spec inherited this from the legacy PUF surface; verify whether eCPS treats it as formula/default before promoting.
        Needs review: current MP spec inherited this from legacy PUF surface, but the manifest questions eCPS formula/default
        treatment.'
  other_medical_expenses:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  other_type_retirement_account_distributions:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  over_the_counter_health_expenses:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  own_children_in_household:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  partnership_s_corp_income:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  partnership_s_corp_income_would_be_qualified:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  partnership_se_income:
    entity: person
    role: puf_imputed
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF; CPS keep half may pass through unless explicitly overridden.
  person_family_id:
    entity: person
    role: structural_export_contract
    temporary: true
    ecps:
      method: PolicyEngine H5 structural id/link/weight export
      code:
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: eCPS structural entity ids, links, and household_weight export.
    mp_legacy:
      method: PolicyEngine US H5 structural id/link/weight export
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 473-486
        symbol: POLICYENGINE_US_STRUCTURAL_EXPORT_COLUMNS
    mp_spec:
      method: exporter structural entity/link column
      operation:
        kind: structural_export
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Structural columns must be asserted by strict stage/export manifests.
  person_household_id:
    entity: person
    role: structural_export_contract
    temporary: true
    ecps:
      method: PolicyEngine H5 structural id/link/weight export
      code:
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: eCPS structural entity ids, links, and household_weight export.
    mp_legacy:
      method: PolicyEngine US H5 structural id/link/weight export
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 473-486
        symbol: POLICYENGINE_US_STRUCTURAL_EXPORT_COLUMNS
    mp_spec:
      method: exporter structural entity/link column
      operation:
        kind: structural_export
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Structural columns must be asserted by strict stage/export manifests.
  person_id:
    entity: person
    role: structural_export_contract
    temporary: true
    ecps:
      method: PolicyEngine H5 structural id/link/weight export
      code:
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: eCPS structural entity ids, links, and household_weight export.
    mp_legacy:
      method: PolicyEngine US H5 structural id/link/weight export
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 473-486
        symbol: POLICYENGINE_US_STRUCTURAL_EXPORT_COLUMNS
    mp_spec:
      method: exporter structural entity/link column
      operation:
        kind: structural_export
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Structural columns must be asserted by strict stage/export manifests.
  person_marital_unit_id:
    entity: person
    role: structural_export_contract
    temporary: true
    ecps:
      method: PolicyEngine H5 structural id/link/weight export
      code:
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: eCPS structural entity ids, links, and household_weight export.
    mp_legacy:
      method: PolicyEngine US H5 structural id/link/weight export
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 473-486
        symbol: POLICYENGINE_US_STRUCTURAL_EXPORT_COLUMNS
    mp_spec:
      method: exporter structural entity/link column
      operation:
        kind: structural_export
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Structural columns must be asserted by strict stage/export manifests.
  person_spm_unit_id:
    entity: person
    role: structural_export_contract
    temporary: true
    ecps:
      method: PolicyEngine H5 structural id/link/weight export
      code:
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: eCPS structural entity ids, links, and household_weight export.
    mp_legacy:
      method: PolicyEngine US H5 structural id/link/weight export
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 473-486
        symbol: POLICYENGINE_US_STRUCTURAL_EXPORT_COLUMNS
    mp_spec:
      method: exporter structural entity/link column
      operation:
        kind: structural_export
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Structural columns must be asserted by strict stage/export manifests.
  person_tax_unit_id:
    entity: person
    role: structural_export_contract
    temporary: true
    ecps:
      method: PolicyEngine H5 structural id/link/weight export
      code:
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: eCPS structural entity ids, links, and household_weight export.
    mp_legacy:
      method: PolicyEngine US H5 structural id/link/weight export
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 473-486
        symbol: POLICYENGINE_US_STRUCTURAL_EXPORT_COLUMNS
    mp_spec:
      method: exporter structural entity/link column
      operation:
        kind: structural_export
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Structural columns must be asserted by strict stage/export manifests.
  pre_subsidy_rent:
    entity: person
    role: acs_housing_source_imputed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: declared ACS housing source-imputation step
      operation:
        kind: impute
        source: acs
        imputation_step: acs_source_impute
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in the source-impute manifest as ACS rent mapped onto the PolicyEngine pre_subsidy_rent export column.
      notes: ACS donor rent is materialized as pre_subsidy_rent before generic source imputation; no runtime country Python
        is required.
  previous_year_income_available:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  qualified_bdc_income:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  qualified_dividend_income:
    entity: person
    role: puf_imputed
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF; CPS keep half may pass through unless explicitly overridden.
  qualified_reit_and_ptp_income:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  qualified_tuition_expenses:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  real_estate_taxes:
    entity: person
    role: acs_housing_source_imputed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: declared ACS housing source-imputation step
      operation:
        kind: impute
        source: acs
        imputation_step: acs_source_impute
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in the source-impute manifest as an ACS housing target.
      notes: ACS donor real_estate_taxes is imputed by the generic manifest-backed source-impute stage.
  receives_housing_assistance:
    entity: spm_unit
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  receives_wic:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  regular_ira_distributions:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  rental_income:
    entity: person
    role: puf_imputed
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
      notes: MP legacy keeps CPS values for same-name non-overridden columns; this is explicitly called out for known collisions.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: 'Known keep-CPS collision: the synthetic half must synthesize this variable and must not inherit the CPS canonical
        value. Known keep-CPS collision from Claude manifest; synthetic half must not keep CPS canonical value.'
  rental_income_would_be_qualified:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  reported_has_champva_health_coverage_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: reported_has_champva_health_coverage_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  reported_has_chip_health_coverage_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: reported_has_chip_health_coverage_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  reported_has_direct_purchase_health_coverage_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: reported_has_direct_purchase_health_coverage_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  reported_has_employer_sponsored_health_coverage_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: reported_has_employer_sponsored_health_coverage_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  reported_has_indian_health_service_coverage_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: reported_has_indian_health_service_coverage_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  reported_has_marketplace_health_coverage_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: reported_has_marketplace_health_coverage_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  reported_has_means_tested_health_coverage_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: reported_has_means_tested_health_coverage_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  reported_has_medicaid_health_coverage_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: reported_has_medicaid_health_coverage_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  reported_has_medicare_health_coverage_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: reported_has_medicare_health_coverage_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  reported_has_multiple_health_coverage_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: reported_has_multiple_health_coverage_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  reported_has_non_marketplace_direct_purchase_health_coverage_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: reported_has_non_marketplace_direct_purchase_health_coverage_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  reported_has_other_means_tested_health_coverage_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: reported_has_other_means_tested_health_coverage_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  reported_has_private_health_coverage_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: reported_has_private_health_coverage_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  reported_has_public_health_coverage_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: reported_has_public_health_coverage_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  reported_has_subsidized_marketplace_health_coverage_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: reported_has_subsidized_marketplace_health_coverage_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  reported_has_tricare_health_coverage_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: reported_has_tricare_health_coverage_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  reported_has_unsubsidized_marketplace_health_coverage_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: reported_has_unsubsidized_marketplace_health_coverage_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  reported_has_va_health_coverage_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: reported_has_va_health_coverage_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  reported_is_insured_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: reported_is_insured_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  reported_is_uninsured_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: reported_is_uninsured_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  reported_owns_employer_sponsored_health_insurance_at_interview:
    entity: person
    role: reported_health_coverage_contract
    temporary: true
    ecps:
      method: CPS current-health coverage recode/passthrough
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: eCPS CPS ASEC current-health coverage mappings and recodes.
    mp_legacy:
      method: CPS current-health coverage recode or default fallback
      code:
      - path: src/microplex_us/data_sources/cps.py
        lines: 43-87,1540-1650,2293-2298
        symbol: CURRENT_HEALTH_COVERAGE_REPORTED_VAR_MAP / CURRENT_HEALTH_COVERAGE_RULE_INPUT_ALIAS_MAP
      - path: src/microplex_us/policyengine/us.py
        lines: 604-623,752-775
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
    mp_spec:
      method: passthrough/derive from CPS current-health coverage source columns
      operation:
        kind: passthrough
        source: cps_asec
        source_column: reported_owns_employer_sponsored_health_insurance_at_interview
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; SourceRegistry CPS loader should own the concrete recode.
      notes: Subagent audit flagged this family as under-specified; all required reported_* leaves are now contract-gated
        in variables.
  roth_401k_contributions:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  roth_401k_contributions_desired:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  roth_ira_contributions:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  roth_ira_contributions_desired:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  roth_ira_distributions:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  salt_refund_income:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  scf_business_equity:
    entity: person
    role: scf_component_contract
    temporary: true
    ecps:
      method: SCF balance-sheet component imputation/reconciliation
      code:
      - path: policyengine_us_data/calibration/source_impute.py
        lines: 668-814
        symbol: impute_scf_variables
        summary: eCPS SCF donor imputation path.
      - path: policyengine_us_data/utils/asset_imputation.py
        lines: 40-86,373-490
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / NET_WORTH_COMPONENT_SIGNS / rebalance_scf_net_worth_components
    mp_legacy:
      method: SCF component leaf default/export support
      code:
      - path: src/microplex_us/variables.py
        lines: 575-606
        symbol: SCF_NET_WORTH_COMPONENT_LEAVES
      - path: src/microplex_us/asset_reconciliation.py
        lines: 38-126
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / compute_net_worth_from_components
      - path: src/microplex_us/policyengine/us.py
        lines: 524-542,725-743
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      notes: Legacy export can still fall back to zero; final spec needs source-imputed component leaves, not default-only
        parity.
    mp_spec:
      method: source=scf component donor imputation plus net-worth reconciliation
      operation:
        kind: impute
        source: scf
        imputation_step: scf_source_impute
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary contract declaration; concrete SCF block still needs declarative SourceRegistry wiring.
      notes: Required because eCPS persists these 19 leaves. Do not treat line-count column parity as enough if these stay
        default-only.
  scf_cash_value_life_insurance:
    entity: person
    role: scf_component_contract
    temporary: true
    ecps:
      method: SCF balance-sheet component imputation/reconciliation
      code:
      - path: policyengine_us_data/calibration/source_impute.py
        lines: 668-814
        symbol: impute_scf_variables
        summary: eCPS SCF donor imputation path.
      - path: policyengine_us_data/utils/asset_imputation.py
        lines: 40-86,373-490
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / NET_WORTH_COMPONENT_SIGNS / rebalance_scf_net_worth_components
    mp_legacy:
      method: SCF component leaf default/export support
      code:
      - path: src/microplex_us/variables.py
        lines: 575-606
        symbol: SCF_NET_WORTH_COMPONENT_LEAVES
      - path: src/microplex_us/asset_reconciliation.py
        lines: 38-126
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / compute_net_worth_from_components
      - path: src/microplex_us/policyengine/us.py
        lines: 524-542,725-743
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      notes: Legacy export can still fall back to zero; final spec needs source-imputed component leaves, not default-only
        parity.
    mp_spec:
      method: source=scf component donor imputation plus net-worth reconciliation
      operation:
        kind: impute
        source: scf
        imputation_step: scf_source_impute
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary contract declaration; concrete SCF block still needs declarative SourceRegistry wiring.
      notes: Required because eCPS persists these 19 leaves. Do not treat line-count column parity as enough if these stay
        default-only.
  scf_certificates_of_deposit:
    entity: person
    role: scf_component_contract
    temporary: true
    ecps:
      method: SCF balance-sheet component imputation/reconciliation
      code:
      - path: policyengine_us_data/calibration/source_impute.py
        lines: 668-814
        symbol: impute_scf_variables
        summary: eCPS SCF donor imputation path.
      - path: policyengine_us_data/utils/asset_imputation.py
        lines: 40-86,373-490
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / NET_WORTH_COMPONENT_SIGNS / rebalance_scf_net_worth_components
    mp_legacy:
      method: SCF component leaf default/export support
      code:
      - path: src/microplex_us/variables.py
        lines: 575-606
        symbol: SCF_NET_WORTH_COMPONENT_LEAVES
      - path: src/microplex_us/asset_reconciliation.py
        lines: 38-126
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / compute_net_worth_from_components
      - path: src/microplex_us/policyengine/us.py
        lines: 524-542,725-743
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      notes: Legacy export can still fall back to zero; final spec needs source-imputed component leaves, not default-only
        parity.
    mp_spec:
      method: source=scf component donor imputation plus net-worth reconciliation
      operation:
        kind: impute
        source: scf
        imputation_step: scf_source_impute
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary contract declaration; concrete SCF block still needs declarative SourceRegistry wiring.
      notes: Required because eCPS persists these 19 leaves. Do not treat line-count column parity as enough if these stay
        default-only.
  scf_credit_card_debt:
    entity: person
    role: scf_component_contract
    temporary: true
    ecps:
      method: SCF balance-sheet component imputation/reconciliation
      code:
      - path: policyengine_us_data/calibration/source_impute.py
        lines: 668-814
        symbol: impute_scf_variables
        summary: eCPS SCF donor imputation path.
      - path: policyengine_us_data/utils/asset_imputation.py
        lines: 40-86,373-490
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / NET_WORTH_COMPONENT_SIGNS / rebalance_scf_net_worth_components
    mp_legacy:
      method: SCF component leaf default/export support
      code:
      - path: src/microplex_us/variables.py
        lines: 575-606
        symbol: SCF_NET_WORTH_COMPONENT_LEAVES
      - path: src/microplex_us/asset_reconciliation.py
        lines: 38-126
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / compute_net_worth_from_components
      - path: src/microplex_us/policyengine/us.py
        lines: 524-542,725-743
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      notes: Legacy export can still fall back to zero; final spec needs source-imputed component leaves, not default-only
        parity.
    mp_spec:
      method: source=scf component donor imputation plus net-worth reconciliation
      operation:
        kind: impute
        source: scf
        imputation_step: scf_source_impute
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary contract declaration; concrete SCF block still needs declarative SourceRegistry wiring.
      notes: Required because eCPS persists these 19 leaves. Do not treat line-count column parity as enough if these stay
        default-only.
  scf_mortgage_debt:
    entity: person
    role: scf_component_contract
    temporary: true
    ecps:
      method: SCF balance-sheet component imputation/reconciliation
      code:
      - path: policyengine_us_data/calibration/source_impute.py
        lines: 668-814
        symbol: impute_scf_variables
        summary: eCPS SCF donor imputation path.
      - path: policyengine_us_data/utils/asset_imputation.py
        lines: 40-86,373-490
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / NET_WORTH_COMPONENT_SIGNS / rebalance_scf_net_worth_components
    mp_legacy:
      method: SCF component leaf default/export support
      code:
      - path: src/microplex_us/variables.py
        lines: 575-606
        symbol: SCF_NET_WORTH_COMPONENT_LEAVES
      - path: src/microplex_us/asset_reconciliation.py
        lines: 38-126
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / compute_net_worth_from_components
      - path: src/microplex_us/policyengine/us.py
        lines: 524-542,725-743
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      notes: Legacy export can still fall back to zero; final spec needs source-imputed component leaves, not default-only
        parity.
    mp_spec:
      method: source=scf component donor imputation plus net-worth reconciliation
      operation:
        kind: impute
        source: scf
        imputation_step: scf_source_impute
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary contract declaration; concrete SCF block still needs declarative SourceRegistry wiring.
      notes: Required because eCPS persists these 19 leaves. Do not treat line-count column parity as enough if these stay
        default-only.
  scf_nonresidential_real_estate_equity:
    entity: person
    role: scf_component_contract
    temporary: true
    ecps:
      method: SCF balance-sheet component imputation/reconciliation
      code:
      - path: policyengine_us_data/calibration/source_impute.py
        lines: 668-814
        symbol: impute_scf_variables
        summary: eCPS SCF donor imputation path.
      - path: policyengine_us_data/utils/asset_imputation.py
        lines: 40-86,373-490
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / NET_WORTH_COMPONENT_SIGNS / rebalance_scf_net_worth_components
    mp_legacy:
      method: SCF component leaf default/export support
      code:
      - path: src/microplex_us/variables.py
        lines: 575-606
        symbol: SCF_NET_WORTH_COMPONENT_LEAVES
      - path: src/microplex_us/asset_reconciliation.py
        lines: 38-126
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / compute_net_worth_from_components
      - path: src/microplex_us/policyengine/us.py
        lines: 524-542,725-743
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      notes: Legacy export can still fall back to zero; final spec needs source-imputed component leaves, not default-only
        parity.
    mp_spec:
      method: source=scf component donor imputation plus net-worth reconciliation
      operation:
        kind: impute
        source: scf
        imputation_step: scf_source_impute
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary contract declaration; concrete SCF block still needs declarative SourceRegistry wiring.
      notes: Required because eCPS persists these 19 leaves. Do not treat line-count column parity as enough if these stay
        default-only.
  scf_other_debt:
    entity: person
    role: scf_component_contract
    temporary: true
    ecps:
      method: SCF balance-sheet component imputation/reconciliation
      code:
      - path: policyengine_us_data/calibration/source_impute.py
        lines: 668-814
        symbol: impute_scf_variables
        summary: eCPS SCF donor imputation path.
      - path: policyengine_us_data/utils/asset_imputation.py
        lines: 40-86,373-490
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / NET_WORTH_COMPONENT_SIGNS / rebalance_scf_net_worth_components
    mp_legacy:
      method: SCF component leaf default/export support
      code:
      - path: src/microplex_us/variables.py
        lines: 575-606
        symbol: SCF_NET_WORTH_COMPONENT_LEAVES
      - path: src/microplex_us/asset_reconciliation.py
        lines: 38-126
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / compute_net_worth_from_components
      - path: src/microplex_us/policyengine/us.py
        lines: 524-542,725-743
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      notes: Legacy export can still fall back to zero; final spec needs source-imputed component leaves, not default-only
        parity.
    mp_spec:
      method: source=scf component donor imputation plus net-worth reconciliation
      operation:
        kind: impute
        source: scf
        imputation_step: scf_source_impute
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary contract declaration; concrete SCF block still needs declarative SourceRegistry wiring.
      notes: Required because eCPS persists these 19 leaves. Do not treat line-count column parity as enough if these stay
        default-only.
  scf_other_financial_assets:
    entity: person
    role: scf_component_contract
    temporary: true
    ecps:
      method: SCF balance-sheet component imputation/reconciliation
      code:
      - path: policyengine_us_data/calibration/source_impute.py
        lines: 668-814
        symbol: impute_scf_variables
        summary: eCPS SCF donor imputation path.
      - path: policyengine_us_data/utils/asset_imputation.py
        lines: 40-86,373-490
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / NET_WORTH_COMPONENT_SIGNS / rebalance_scf_net_worth_components
    mp_legacy:
      method: SCF component leaf default/export support
      code:
      - path: src/microplex_us/variables.py
        lines: 575-606
        symbol: SCF_NET_WORTH_COMPONENT_LEAVES
      - path: src/microplex_us/asset_reconciliation.py
        lines: 38-126
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / compute_net_worth_from_components
      - path: src/microplex_us/policyengine/us.py
        lines: 524-542,725-743
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      notes: Legacy export can still fall back to zero; final spec needs source-imputed component leaves, not default-only
        parity.
    mp_spec:
      method: source=scf component donor imputation plus net-worth reconciliation
      operation:
        kind: impute
        source: scf
        imputation_step: scf_source_impute
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary contract declaration; concrete SCF block still needs declarative SourceRegistry wiring.
      notes: Required because eCPS persists these 19 leaves. Do not treat line-count column parity as enough if these stay
        default-only.
  scf_other_installment_debt:
    entity: person
    role: scf_component_contract
    temporary: true
    ecps:
      method: SCF balance-sheet component imputation/reconciliation
      code:
      - path: policyengine_us_data/calibration/source_impute.py
        lines: 668-814
        symbol: impute_scf_variables
        summary: eCPS SCF donor imputation path.
      - path: policyengine_us_data/utils/asset_imputation.py
        lines: 40-86,373-490
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / NET_WORTH_COMPONENT_SIGNS / rebalance_scf_net_worth_components
    mp_legacy:
      method: SCF component leaf default/export support
      code:
      - path: src/microplex_us/variables.py
        lines: 575-606
        symbol: SCF_NET_WORTH_COMPONENT_LEAVES
      - path: src/microplex_us/asset_reconciliation.py
        lines: 38-126
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / compute_net_worth_from_components
      - path: src/microplex_us/policyengine/us.py
        lines: 524-542,725-743
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      notes: Legacy export can still fall back to zero; final spec needs source-imputed component leaves, not default-only
        parity.
    mp_spec:
      method: source=scf component donor imputation plus net-worth reconciliation
      operation:
        kind: impute
        source: scf
        imputation_step: scf_source_impute
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary contract declaration; concrete SCF block still needs declarative SourceRegistry wiring.
      notes: Required because eCPS persists these 19 leaves. Do not treat line-count column parity as enough if these stay
        default-only.
  scf_other_lines_of_credit:
    entity: person
    role: scf_component_contract
    temporary: true
    ecps:
      method: SCF balance-sheet component imputation/reconciliation
      code:
      - path: policyengine_us_data/calibration/source_impute.py
        lines: 668-814
        symbol: impute_scf_variables
        summary: eCPS SCF donor imputation path.
      - path: policyengine_us_data/utils/asset_imputation.py
        lines: 40-86,373-490
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / NET_WORTH_COMPONENT_SIGNS / rebalance_scf_net_worth_components
    mp_legacy:
      method: SCF component leaf default/export support
      code:
      - path: src/microplex_us/variables.py
        lines: 575-606
        symbol: SCF_NET_WORTH_COMPONENT_LEAVES
      - path: src/microplex_us/asset_reconciliation.py
        lines: 38-126
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / compute_net_worth_from_components
      - path: src/microplex_us/policyengine/us.py
        lines: 524-542,725-743
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      notes: Legacy export can still fall back to zero; final spec needs source-imputed component leaves, not default-only
        parity.
    mp_spec:
      method: source=scf component donor imputation plus net-worth reconciliation
      operation:
        kind: impute
        source: scf
        imputation_step: scf_source_impute
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary contract declaration; concrete SCF block still needs declarative SourceRegistry wiring.
      notes: Required because eCPS persists these 19 leaves. Do not treat line-count column parity as enough if these stay
        default-only.
  scf_other_managed_assets:
    entity: person
    role: scf_component_contract
    temporary: true
    ecps:
      method: SCF balance-sheet component imputation/reconciliation
      code:
      - path: policyengine_us_data/calibration/source_impute.py
        lines: 668-814
        symbol: impute_scf_variables
        summary: eCPS SCF donor imputation path.
      - path: policyengine_us_data/utils/asset_imputation.py
        lines: 40-86,373-490
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / NET_WORTH_COMPONENT_SIGNS / rebalance_scf_net_worth_components
    mp_legacy:
      method: SCF component leaf default/export support
      code:
      - path: src/microplex_us/variables.py
        lines: 575-606
        symbol: SCF_NET_WORTH_COMPONENT_LEAVES
      - path: src/microplex_us/asset_reconciliation.py
        lines: 38-126
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / compute_net_worth_from_components
      - path: src/microplex_us/policyengine/us.py
        lines: 524-542,725-743
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      notes: Legacy export can still fall back to zero; final spec needs source-imputed component leaves, not default-only
        parity.
    mp_spec:
      method: source=scf component donor imputation plus net-worth reconciliation
      operation:
        kind: impute
        source: scf
        imputation_step: scf_source_impute
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary contract declaration; concrete SCF block still needs declarative SourceRegistry wiring.
      notes: Required because eCPS persists these 19 leaves. Do not treat line-count column parity as enough if these stay
        default-only.
  scf_other_nonfinancial_assets:
    entity: person
    role: scf_component_contract
    temporary: true
    ecps:
      method: SCF balance-sheet component imputation/reconciliation
      code:
      - path: policyengine_us_data/calibration/source_impute.py
        lines: 668-814
        symbol: impute_scf_variables
        summary: eCPS SCF donor imputation path.
      - path: policyengine_us_data/utils/asset_imputation.py
        lines: 40-86,373-490
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / NET_WORTH_COMPONENT_SIGNS / rebalance_scf_net_worth_components
    mp_legacy:
      method: SCF component leaf default/export support
      code:
      - path: src/microplex_us/variables.py
        lines: 575-606
        symbol: SCF_NET_WORTH_COMPONENT_LEAVES
      - path: src/microplex_us/asset_reconciliation.py
        lines: 38-126
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / compute_net_worth_from_components
      - path: src/microplex_us/policyengine/us.py
        lines: 524-542,725-743
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      notes: Legacy export can still fall back to zero; final spec needs source-imputed component leaves, not default-only
        parity.
    mp_spec:
      method: source=scf component donor imputation plus net-worth reconciliation
      operation:
        kind: impute
        source: scf
        imputation_step: scf_source_impute
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary contract declaration; concrete SCF block still needs declarative SourceRegistry wiring.
      notes: Required because eCPS persists these 19 leaves. Do not treat line-count column parity as enough if these stay
        default-only.
  scf_other_residential_debt:
    entity: person
    role: scf_component_contract
    temporary: true
    ecps:
      method: SCF balance-sheet component imputation/reconciliation
      code:
      - path: policyengine_us_data/calibration/source_impute.py
        lines: 668-814
        symbol: impute_scf_variables
        summary: eCPS SCF donor imputation path.
      - path: policyengine_us_data/utils/asset_imputation.py
        lines: 40-86,373-490
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / NET_WORTH_COMPONENT_SIGNS / rebalance_scf_net_worth_components
    mp_legacy:
      method: SCF component leaf default/export support
      code:
      - path: src/microplex_us/variables.py
        lines: 575-606
        symbol: SCF_NET_WORTH_COMPONENT_LEAVES
      - path: src/microplex_us/asset_reconciliation.py
        lines: 38-126
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / compute_net_worth_from_components
      - path: src/microplex_us/policyengine/us.py
        lines: 524-542,725-743
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      notes: Legacy export can still fall back to zero; final spec needs source-imputed component leaves, not default-only
        parity.
    mp_spec:
      method: source=scf component donor imputation plus net-worth reconciliation
      operation:
        kind: impute
        source: scf
        imputation_step: scf_source_impute
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary contract declaration; concrete SCF block still needs declarative SourceRegistry wiring.
      notes: Required because eCPS persists these 19 leaves. Do not treat line-count column parity as enough if these stay
        default-only.
  scf_other_residential_real_estate:
    entity: person
    role: scf_component_contract
    temporary: true
    ecps:
      method: SCF balance-sheet component imputation/reconciliation
      code:
      - path: policyengine_us_data/calibration/source_impute.py
        lines: 668-814
        symbol: impute_scf_variables
        summary: eCPS SCF donor imputation path.
      - path: policyengine_us_data/utils/asset_imputation.py
        lines: 40-86,373-490
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / NET_WORTH_COMPONENT_SIGNS / rebalance_scf_net_worth_components
    mp_legacy:
      method: SCF component leaf default/export support
      code:
      - path: src/microplex_us/variables.py
        lines: 575-606
        symbol: SCF_NET_WORTH_COMPONENT_LEAVES
      - path: src/microplex_us/asset_reconciliation.py
        lines: 38-126
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / compute_net_worth_from_components
      - path: src/microplex_us/policyengine/us.py
        lines: 524-542,725-743
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      notes: Legacy export can still fall back to zero; final spec needs source-imputed component leaves, not default-only
        parity.
    mp_spec:
      method: source=scf component donor imputation plus net-worth reconciliation
      operation:
        kind: impute
        source: scf
        imputation_step: scf_source_impute
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary contract declaration; concrete SCF block still needs declarative SourceRegistry wiring.
      notes: Required because eCPS persists these 19 leaves. Do not treat line-count column parity as enough if these stay
        default-only.
  scf_primary_residence_value:
    entity: person
    role: scf_component_contract
    temporary: true
    ecps:
      method: SCF balance-sheet component imputation/reconciliation
      code:
      - path: policyengine_us_data/calibration/source_impute.py
        lines: 668-814
        symbol: impute_scf_variables
        summary: eCPS SCF donor imputation path.
      - path: policyengine_us_data/utils/asset_imputation.py
        lines: 40-86,373-490
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / NET_WORTH_COMPONENT_SIGNS / rebalance_scf_net_worth_components
    mp_legacy:
      method: SCF component leaf default/export support
      code:
      - path: src/microplex_us/variables.py
        lines: 575-606
        symbol: SCF_NET_WORTH_COMPONENT_LEAVES
      - path: src/microplex_us/asset_reconciliation.py
        lines: 38-126
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / compute_net_worth_from_components
      - path: src/microplex_us/policyengine/us.py
        lines: 524-542,725-743
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      notes: Legacy export can still fall back to zero; final spec needs source-imputed component leaves, not default-only
        parity.
    mp_spec:
      method: source=scf component donor imputation plus net-worth reconciliation
      operation:
        kind: impute
        source: scf
        imputation_step: scf_source_impute
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary contract declaration; concrete SCF block still needs declarative SourceRegistry wiring.
      notes: Required because eCPS persists these 19 leaves. Do not treat line-count column parity as enough if these stay
        default-only.
  scf_retirement_assets:
    entity: person
    role: scf_component_contract
    temporary: true
    ecps:
      method: SCF balance-sheet component imputation/reconciliation
      code:
      - path: policyengine_us_data/calibration/source_impute.py
        lines: 668-814
        symbol: impute_scf_variables
        summary: eCPS SCF donor imputation path.
      - path: policyengine_us_data/utils/asset_imputation.py
        lines: 40-86,373-490
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / NET_WORTH_COMPONENT_SIGNS / rebalance_scf_net_worth_components
    mp_legacy:
      method: SCF component leaf default/export support
      code:
      - path: src/microplex_us/variables.py
        lines: 575-606
        symbol: SCF_NET_WORTH_COMPONENT_LEAVES
      - path: src/microplex_us/asset_reconciliation.py
        lines: 38-126
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / compute_net_worth_from_components
      - path: src/microplex_us/policyengine/us.py
        lines: 524-542,725-743
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      notes: Legacy export can still fall back to zero; final spec needs source-imputed component leaves, not default-only
        parity.
    mp_spec:
      method: source=scf component donor imputation plus net-worth reconciliation
      operation:
        kind: impute
        source: scf
        imputation_step: scf_source_impute
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary contract declaration; concrete SCF block still needs declarative SourceRegistry wiring.
      notes: Required because eCPS persists these 19 leaves. Do not treat line-count column parity as enough if these stay
        default-only.
  scf_savings_bonds:
    entity: person
    role: scf_component_contract
    temporary: true
    ecps:
      method: SCF balance-sheet component imputation/reconciliation
      code:
      - path: policyengine_us_data/calibration/source_impute.py
        lines: 668-814
        symbol: impute_scf_variables
        summary: eCPS SCF donor imputation path.
      - path: policyengine_us_data/utils/asset_imputation.py
        lines: 40-86,373-490
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / NET_WORTH_COMPONENT_SIGNS / rebalance_scf_net_worth_components
    mp_legacy:
      method: SCF component leaf default/export support
      code:
      - path: src/microplex_us/variables.py
        lines: 575-606
        symbol: SCF_NET_WORTH_COMPONENT_LEAVES
      - path: src/microplex_us/asset_reconciliation.py
        lines: 38-126
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / compute_net_worth_from_components
      - path: src/microplex_us/policyengine/us.py
        lines: 524-542,725-743
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      notes: Legacy export can still fall back to zero; final spec needs source-imputed component leaves, not default-only
        parity.
    mp_spec:
      method: source=scf component donor imputation plus net-worth reconciliation
      operation:
        kind: impute
        source: scf
        imputation_step: scf_source_impute
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary contract declaration; concrete SCF block still needs declarative SourceRegistry wiring.
      notes: Required because eCPS persists these 19 leaves. Do not treat line-count column parity as enough if these stay
        default-only.
  scf_student_loan_debt:
    entity: person
    role: scf_component_contract
    temporary: true
    ecps:
      method: SCF balance-sheet component imputation/reconciliation
      code:
      - path: policyengine_us_data/calibration/source_impute.py
        lines: 668-814
        symbol: impute_scf_variables
        summary: eCPS SCF donor imputation path.
      - path: policyengine_us_data/utils/asset_imputation.py
        lines: 40-86,373-490
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / NET_WORTH_COMPONENT_SIGNS / rebalance_scf_net_worth_components
    mp_legacy:
      method: SCF component leaf default/export support
      code:
      - path: src/microplex_us/variables.py
        lines: 575-606
        symbol: SCF_NET_WORTH_COMPONENT_LEAVES
      - path: src/microplex_us/asset_reconciliation.py
        lines: 38-126
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / compute_net_worth_from_components
      - path: src/microplex_us/policyengine/us.py
        lines: 524-542,725-743
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      notes: Legacy export can still fall back to zero; final spec needs source-imputed component leaves, not default-only
        parity.
    mp_spec:
      method: source=scf component donor imputation plus net-worth reconciliation
      operation:
        kind: impute
        source: scf
        imputation_step: scf_source_impute
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary contract declaration; concrete SCF block still needs declarative SourceRegistry wiring.
      notes: Required because eCPS persists these 19 leaves. Do not treat line-count column parity as enough if these stay
        default-only.
  scf_vehicle_installment_debt:
    entity: person
    role: scf_component_contract
    temporary: true
    ecps:
      method: SCF balance-sheet component imputation/reconciliation
      code:
      - path: policyengine_us_data/calibration/source_impute.py
        lines: 668-814
        symbol: impute_scf_variables
        summary: eCPS SCF donor imputation path.
      - path: policyengine_us_data/utils/asset_imputation.py
        lines: 40-86,373-490
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / NET_WORTH_COMPONENT_SIGNS / rebalance_scf_net_worth_components
    mp_legacy:
      method: SCF component leaf default/export support
      code:
      - path: src/microplex_us/variables.py
        lines: 575-606
        symbol: SCF_NET_WORTH_COMPONENT_LEAVES
      - path: src/microplex_us/asset_reconciliation.py
        lines: 38-126
        symbol: SCF_NET_WORTH_COMPONENT_TARGETS / compute_net_worth_from_components
      - path: src/microplex_us/policyengine/us.py
        lines: 524-542,725-743
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      notes: Legacy export can still fall back to zero; final spec needs source-imputed component leaves, not default-only
        parity.
    mp_spec:
      method: source=scf component donor imputation plus net-worth reconciliation
      operation:
        kind: impute
        source: scf
        imputation_step: scf_source_impute
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary contract declaration; concrete SCF block still needs declarative SourceRegistry wiring.
      notes: Required because eCPS persists these 19 leaves. Do not treat line-count column parity as enough if these stay
        default-only.
  second_home_mortgage_balance:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  second_home_mortgage_interest:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  second_home_mortgage_origination_year:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  selected_marketplace_plan_benchmark_ratio:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  self_employed_pension_contributions:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  self_employed_pension_contributions_desired:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  self_employment_income_before_lsr:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  self_employment_income_last_year:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  self_employment_income_would_be_qualified:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  short_term_capital_gains:
    entity: person
    role: puf_imputed
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF; CPS keep half may pass through unless explicitly overridden.
  social_security_dependents:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  social_security_disability:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  social_security_retirement:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  social_security_survivors:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  spm_unit_capped_work_childcare_expenses:
    entity: spm_unit
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  spm_unit_energy_subsidy:
    entity: spm_unit
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  spm_unit_id:
    entity: spm_unit
    role: structural_export_contract
    temporary: true
    ecps:
      method: PolicyEngine H5 structural id/link/weight export
      code:
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: eCPS structural entity ids, links, and household_weight export.
    mp_legacy:
      method: PolicyEngine US H5 structural id/link/weight export
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 473-486
        symbol: POLICYENGINE_US_STRUCTURAL_EXPORT_COLUMNS
    mp_spec:
      method: exporter structural entity/link column
      operation:
        kind: structural_export
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Structural columns must be asserted by strict stage/export manifests.
  spm_unit_pre_subsidy_childcare_expenses:
    entity: spm_unit
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  spm_unit_tenure_type:
    entity: spm_unit
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  ssn_card_type:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  sstb_self_employment_income_before_lsr:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  sstb_self_employment_income_would_be_qualified:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  sstb_unadjusted_basis_qualified_property:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  sstb_w2_wages_from_qualified_business:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  state_fips:
    entity: household
    role: geography_export_contract
    temporary: true
    ecps:
      method: eCPS geography assignment/export encoding
      code:
      - path: policyengine_us_data/storage/enhanced_cps/clone_and_assign.py
        lines: 55,114-151
        symbol: assign_random_geography
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 269-289
        summary: Fixed-width block/tract/county export encoding.
      notes: eCPS emits fixed-width byte strings for block/tract/county geographies.
    mp_legacy:
      method: Microplex census block assignment/export derivation
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 491-587,3999-4023
        symbol: _attach_household_census_geographies / _ensure_household_in_nyc
      - path: src/microplex_us/geography.py
        lines: 403-527,550-647
        symbol: BlockGeography
      notes: Manifest flagged possible encoding/export divergence; exporter must preserve eCPS-compatible widths.
    mp_spec:
      method: geography assignment/exporter derived geography with eCPS-compatible encoding
      operation:
        kind: encode_geoid
        source: geography
        encoding: S2
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration for future geography assignment/exporter rules.
  stock_assets:
    entity: person
    role: sipp_asset_source_imputed
    temporary: true
    ecps:
      method: SIPP asset donor QRF imputation
      code:
      - path: policyengine_us_data/calibration/source_impute.py
        lines: 37-42,562-657
        symbol: SIPP_ASSETS_VARIABLES / impute_sipp_assets
    mp_legacy:
      method: SIPP donor survey/source-impute target
      code:
      - path: src/microplex_us/pe_source_impute_specs.py
        summary: MP legacy PE-source donor block specs.
      - path: src/microplex_us/data_sources/donor_surveys.py
        lines: 1236-1291
        symbol: SIPPSourceProvider / SIPPAssetsSourceProvider
    mp_spec:
      method: source=sipp asset donor imputation
      operation:
        kind: impute
        source: sipp
        imputation_step: sipp_source_impute
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration for future SourceRegistry-backed source-imputation step.
  strike_benefits:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  student_loan_interest:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  survivor_benefits:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  takes_up_aca_if_eligible:
    entity: tax_unit
    role: takeup_input_contract
    temporary: true
    ecps:
      method: PolicyEngine takeup input/default or eCPS reported-benefit driver
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Enhanced CPS benefit/takeup source inputs and defaulted takeup leaves.
    mp_legacy:
      method: deterministic Microplex takeup helper/default
      code:
      - path: src/microplex_us/policyengine/takeup.py
        lines: 14-115,171-253,694-728
        summary: Default takeup rates and feature families.
      - path: src/microplex_us/pipelines/us.py
        lines: 9682-10337
        summary: Legacy pipeline materialization of deterministic takeup inputs.
    mp_spec:
      method: rerandomized_takeup handler or deterministic takeup transform
      operation:
        kind: rerandomize_takeup
        handler: policyengine_us_takeup
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration until takeup transforms are fully serialized.
      notes: Must be explicit because forbidden *_reported takeup columns are not substitutes for required takeup input leaves.
  takes_up_dc_ptc:
    entity: tax_unit
    role: takeup_input_contract
    temporary: true
    ecps:
      method: PolicyEngine takeup input/default or eCPS reported-benefit driver
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Enhanced CPS benefit/takeup source inputs and defaulted takeup leaves.
    mp_legacy:
      method: deterministic Microplex takeup helper/default
      code:
      - path: src/microplex_us/policyengine/takeup.py
        lines: 14-115,171-253,694-728
        summary: Default takeup rates and feature families.
      - path: src/microplex_us/pipelines/us.py
        lines: 9682-10337
        summary: Legacy pipeline materialization of deterministic takeup inputs.
    mp_spec:
      method: rerandomized_takeup handler or deterministic takeup transform
      operation:
        kind: rerandomize_takeup
        handler: policyengine_us_takeup
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration until takeup transforms are fully serialized.
      notes: Must be explicit because forbidden *_reported takeup columns are not substitutes for required takeup input leaves.
  takes_up_early_head_start_if_eligible:
    entity: person
    role: takeup_input_contract
    temporary: true
    ecps:
      method: PolicyEngine takeup input/default or eCPS reported-benefit driver
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Enhanced CPS benefit/takeup source inputs and defaulted takeup leaves.
    mp_legacy:
      method: deterministic Microplex takeup helper/default
      code:
      - path: src/microplex_us/policyengine/takeup.py
        lines: 14-115,171-253,694-728
        summary: Default takeup rates and feature families.
      - path: src/microplex_us/pipelines/us.py
        lines: 9682-10337
        summary: Legacy pipeline materialization of deterministic takeup inputs.
    mp_spec:
      method: rerandomized_takeup handler or deterministic takeup transform
      operation:
        kind: rerandomize_takeup
        handler: policyengine_us_takeup
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration until takeup transforms are fully serialized.
      notes: Must be explicit because forbidden *_reported takeup columns are not substitutes for required takeup input leaves.
  takes_up_eitc:
    entity: tax_unit
    role: takeup_input_contract
    temporary: true
    ecps:
      method: PolicyEngine takeup input/default or eCPS reported-benefit driver
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Enhanced CPS benefit/takeup source inputs and defaulted takeup leaves.
    mp_legacy:
      method: deterministic Microplex takeup helper/default
      code:
      - path: src/microplex_us/policyengine/takeup.py
        lines: 14-115,171-253,694-728
        summary: Default takeup rates and feature families.
      - path: src/microplex_us/pipelines/us.py
        lines: 9682-10337
        summary: Legacy pipeline materialization of deterministic takeup inputs.
    mp_spec:
      method: rerandomized_takeup handler or deterministic takeup transform
      operation:
        kind: rerandomize_takeup
        handler: policyengine_us_takeup
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration until takeup transforms are fully serialized.
      notes: Must be explicit because forbidden *_reported takeup columns are not substitutes for required takeup input leaves.
  takes_up_head_start_if_eligible:
    entity: person
    role: takeup_input_contract
    temporary: true
    ecps:
      method: PolicyEngine takeup input/default or eCPS reported-benefit driver
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Enhanced CPS benefit/takeup source inputs and defaulted takeup leaves.
    mp_legacy:
      method: deterministic Microplex takeup helper/default
      code:
      - path: src/microplex_us/policyengine/takeup.py
        lines: 14-115,171-253,694-728
        summary: Default takeup rates and feature families.
      - path: src/microplex_us/pipelines/us.py
        lines: 9682-10337
        summary: Legacy pipeline materialization of deterministic takeup inputs.
    mp_spec:
      method: rerandomized_takeup handler or deterministic takeup transform
      operation:
        kind: rerandomize_takeup
        handler: policyengine_us_takeup
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration until takeup transforms are fully serialized.
      notes: Must be explicit because forbidden *_reported takeup columns are not substitutes for required takeup input leaves.
  takes_up_housing_assistance_if_eligible:
    entity: spm_unit
    role: takeup_input_contract
    temporary: true
    ecps:
      method: PolicyEngine takeup input/default or eCPS reported-benefit driver
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Enhanced CPS benefit/takeup source inputs and defaulted takeup leaves.
    mp_legacy:
      method: deterministic Microplex takeup helper/default
      code:
      - path: src/microplex_us/policyengine/takeup.py
        lines: 14-115,171-253,694-728
        summary: Default takeup rates and feature families.
      - path: src/microplex_us/pipelines/us.py
        lines: 9682-10337
        summary: Legacy pipeline materialization of deterministic takeup inputs.
    mp_spec:
      method: rerandomized_takeup handler or deterministic takeup transform
      operation:
        kind: rerandomize_takeup
        handler: policyengine_us_takeup
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration until takeup transforms are fully serialized.
      notes: Must be explicit because forbidden *_reported takeup columns are not substitutes for required takeup input leaves.
  takes_up_medicaid_if_eligible:
    entity: person
    role: takeup_input_contract
    temporary: true
    ecps:
      method: PolicyEngine takeup input/default or eCPS reported-benefit driver
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Enhanced CPS benefit/takeup source inputs and defaulted takeup leaves.
    mp_legacy:
      method: deterministic Microplex takeup helper/default
      code:
      - path: src/microplex_us/policyengine/takeup.py
        lines: 14-115,171-253,694-728
        summary: Default takeup rates and feature families.
      - path: src/microplex_us/pipelines/us.py
        lines: 9682-10337
        summary: Legacy pipeline materialization of deterministic takeup inputs.
    mp_spec:
      method: rerandomized_takeup handler or deterministic takeup transform
      operation:
        kind: rerandomize_takeup
        handler: policyengine_us_takeup
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration until takeup transforms are fully serialized.
      notes: Must be explicit because forbidden *_reported takeup columns are not substitutes for required takeup input leaves.
  takes_up_medicare_if_eligible:
    entity: person
    role: takeup_input_contract
    temporary: true
    ecps:
      method: PolicyEngine takeup input/default or eCPS reported-benefit driver
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Enhanced CPS benefit/takeup source inputs and defaulted takeup leaves.
    mp_legacy:
      method: deterministic Microplex takeup helper/default
      code:
      - path: src/microplex_us/policyengine/takeup.py
        lines: 14-115,171-253,694-728
        summary: Default takeup rates and feature families.
      - path: src/microplex_us/pipelines/us.py
        lines: 9682-10337
        summary: Legacy pipeline materialization of deterministic takeup inputs.
    mp_spec:
      method: rerandomized_takeup handler or deterministic takeup transform
      operation:
        kind: rerandomize_takeup
        handler: policyengine_us_takeup
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration until takeup transforms are fully serialized.
      notes: Must be explicit because forbidden *_reported takeup columns are not substitutes for required takeup input leaves.
  takes_up_snap_if_eligible:
    entity: spm_unit
    role: takeup_input_contract
    temporary: true
    ecps:
      method: PolicyEngine takeup input/default or eCPS reported-benefit driver
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Enhanced CPS benefit/takeup source inputs and defaulted takeup leaves.
    mp_legacy:
      method: deterministic Microplex takeup helper/default
      code:
      - path: src/microplex_us/policyengine/takeup.py
        lines: 14-115,171-253,694-728
        summary: Default takeup rates and feature families.
      - path: src/microplex_us/pipelines/us.py
        lines: 9682-10337
        summary: Legacy pipeline materialization of deterministic takeup inputs.
    mp_spec:
      method: rerandomized_takeup handler or deterministic takeup transform
      operation:
        kind: rerandomize_takeup
        handler: policyengine_us_takeup
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration until takeup transforms are fully serialized.
      notes: Must be explicit because forbidden *_reported takeup columns are not substitutes for required takeup input leaves.
  takes_up_ssi_if_eligible:
    entity: person
    role: takeup_input_contract
    temporary: true
    ecps:
      method: PolicyEngine takeup input/default or eCPS reported-benefit driver
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Enhanced CPS benefit/takeup source inputs and defaulted takeup leaves.
    mp_legacy:
      method: deterministic Microplex takeup helper/default
      code:
      - path: src/microplex_us/policyengine/takeup.py
        lines: 14-115,171-253,694-728
        summary: Default takeup rates and feature families.
      - path: src/microplex_us/pipelines/us.py
        lines: 9682-10337
        summary: Legacy pipeline materialization of deterministic takeup inputs.
    mp_spec:
      method: rerandomized_takeup handler or deterministic takeup transform
      operation:
        kind: rerandomize_takeup
        handler: policyengine_us_takeup
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration until takeup transforms are fully serialized.
      notes: Must be explicit because forbidden *_reported takeup columns are not substitutes for required takeup input leaves.
  takes_up_tanf_if_eligible:
    entity: spm_unit
    role: takeup_input_contract
    temporary: true
    ecps:
      method: PolicyEngine takeup input/default or eCPS reported-benefit driver
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Enhanced CPS benefit/takeup source inputs and defaulted takeup leaves.
    mp_legacy:
      method: deterministic Microplex takeup helper/default
      code:
      - path: src/microplex_us/policyengine/takeup.py
        lines: 14-115,171-253,694-728
        summary: Default takeup rates and feature families.
      - path: src/microplex_us/pipelines/us.py
        lines: 9682-10337
        summary: Legacy pipeline materialization of deterministic takeup inputs.
    mp_spec:
      method: rerandomized_takeup handler or deterministic takeup transform
      operation:
        kind: rerandomize_takeup
        handler: policyengine_us_takeup
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration until takeup transforms are fully serialized.
      notes: Must be explicit because forbidden *_reported takeup columns are not substitutes for required takeup input leaves.
  tax_exempt_401k_distributions:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  tax_exempt_403b_distributions:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  tax_exempt_interest_income:
    entity: person
    role: puf_imputed
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF; CPS keep half may pass through unless explicitly overridden.
  tax_exempt_ira_distributions:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  tax_exempt_private_pension_income:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  tax_exempt_sep_distributions:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  tax_unit_id:
    entity: tax_unit
    role: structural_export_contract
    temporary: true
    ecps:
      method: PolicyEngine H5 structural id/link/weight export
      code:
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: eCPS structural entity ids, links, and household_weight export.
    mp_legacy:
      method: PolicyEngine US H5 structural id/link/weight export
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 473-486
        symbol: POLICYENGINE_US_STRUCTURAL_EXPORT_COLUMNS
    mp_spec:
      method: exporter structural entity/link column
      operation:
        kind: structural_export
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Structural columns must be asserted by strict stage/export manifests.
  taxable_401k_distributions:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  taxable_403b_distributions:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  taxable_interest_income:
    entity: person
    role: puf_imputed
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF; CPS keep half may pass through unless explicitly overridden.
  taxable_ira_distributions:
    entity: person
    role: puf_imputed
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF; CPS keep half may pass through unless explicitly overridden.
  taxable_private_pension_income:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  taxable_sep_distributions:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  taxpayer_id_type:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  tenure_type:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  tip_income:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  tract_geoid:
    entity: household
    role: geography_export_contract
    temporary: true
    ecps:
      method: eCPS geography assignment/export encoding
      code:
      - path: policyengine_us_data/storage/enhanced_cps/clone_and_assign.py
        lines: 55,114-151
        symbol: assign_random_geography
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 269-289
        summary: Fixed-width block/tract/county export encoding.
      notes: eCPS emits fixed-width byte strings for block/tract/county geographies.
    mp_legacy:
      method: Microplex census block assignment/export derivation
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 491-587,3999-4023
        symbol: _attach_household_census_geographies / _ensure_household_in_nyc
      - path: src/microplex_us/geography.py
        lines: 403-527,550-647
        symbol: BlockGeography
      notes: Manifest flagged possible encoding/export divergence; exporter must preserve eCPS-compatible widths.
    mp_spec:
      method: geography assignment/exporter derived geography with eCPS-compatible encoding
      operation:
        kind: encode_geoid
        source: geography
        encoding: S11
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration for future geography assignment/exporter rules.
  traditional_401k_contributions:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  traditional_401k_contributions_desired:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  traditional_ira_contributions:
    entity: person
    role: puf_imputed
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF; CPS keep half may pass through unless explicitly overridden.
  traditional_ira_contributions_desired:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  treasury_tipped_occupation_code:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  unadjusted_basis_qualified_property:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  unemployment_compensation:
    entity: person
    role: cps_passthrough_or_constructed
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: MP legacy export path or default surface
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 360-472,505-687,725-820
        symbol: SAFE_POLICYENGINE_US_EXPORT_VARIABLES / POLICYENGINE_US_EXPORT_DEFAULTS / POLICYENGINE_US_LEGACY_CONTRACT_VARIABLE_ENTITIES
      - path: src/microplex_us/pipelines/export_lineage_manifest.py
        lines: 240-780
        symbol: build_export_lineage_manifest
        summary: Static MP lineage coverage for required eCPS export columns.
      notes: 'Temporary broad provenance: exact MP legacy source/default evidence should be narrowed per variable as the declarative
        spec replaces the imperative path.'
    mp_spec:
      method: contract-required variable pending exact declarative source rule
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: This temporary variables block declares the intended contract surface.
      notes: Must remain represented in the spec until the runtime SourceRegistry/transform/export stage implements the final
        rule.
  unrecaptured_section_1250_gain:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  unreimbursed_business_employee_expenses:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  veterans_benefits:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  w2_wages_from_qualified_business:
    entity: person
    role: puf_imputed_overridden
    temporary: true
    ecps:
      method: PUF QRF imputation
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: PUF support-clone donor imputation surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables.
  weekly_hours_worked_before_lsr:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  weeks_unemployed:
    entity: person
    role: puf_imputed
    temporary: true
    ecps:
      method: PUF weeks-unemployed QRF plus clip/zero-mask
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248,309-392
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES / impute_weeks_unemployed
        summary: eCPS PUF imputation surface and special weeks_unemployed handling.
    mp_legacy:
      method: legacy default/no dedicated imputation
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
        summary: Documents legacy defaults where the support-clone path leaves a column empty.
    mp_spec:
      method: required special transform/glue, not just a default
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and/or temporary variable provenance.
      notes: Requires eCPS-compatible QRF conditioned on taxable_unemployment_compensation, then clip[0,52] and zero-mask
        when unemployment compensation is zero.
  workers_compensation:
    entity: person
    role: legacy_default_needs_source_review
    temporary: true
    ecps:
      method: CPS/eCPS required export contract source path pending exact row-level attribution
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Primary CPS ASEC loader and enhanced-CPS source transformations.
      - path: policyengine_us_data/storage/enhanced_cps/extended_cps.py
        summary: Enhanced CPS export assembly and persisted-input surface.
      notes: 'Temporary broad provenance: contract coverage is enforced now; exact eCPS line references should be tightened
        per variable before removing temporary=True.'
    mp_legacy:
      method: legacy export default fallback
      code:
      - path: src/microplex_us/policyengine/us.py
        lines: 505-687
        symbol: POLICYENGINE_US_EXPORT_DEFAULTS
      notes: Default-only parity is not sufficient for numeric/behavioral parity unless eCPS also defaults this surface.
    mp_spec:
      method: passthrough/derive/default decision pending exact source classification
      operation:
        kind: open_decision
        notes: Exact passthrough, derive, or default rule still needs SourceRegistry classification.
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration; this row prevents silent omission from the spec.
      notes: Use the variable manifest and lineage gate to decide whether this is CPS passthrough, deterministic derive, or
        valid default. MP legacy currently has an export default for this required column; verify source passthrough or transform
        before promotion.
  would_claim_wic:
    entity: person
    role: takeup_input_contract
    temporary: true
    ecps:
      method: PolicyEngine takeup input/default or eCPS reported-benefit driver
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Enhanced CPS benefit/takeup source inputs and defaulted takeup leaves.
    mp_legacy:
      method: deterministic Microplex takeup helper/default
      code:
      - path: src/microplex_us/policyengine/takeup.py
        lines: 14-115,171-253,694-728
        summary: Default takeup rates and feature families.
      - path: src/microplex_us/pipelines/us.py
        lines: 9682-10337
        summary: Legacy pipeline materialization of deterministic takeup inputs.
    mp_spec:
      method: rerandomized_takeup handler or deterministic takeup transform
      operation:
        kind: rerandomize_takeup
        handler: policyengine_us_takeup
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration until takeup transforms are fully serialized.
      notes: Must be explicit because forbidden *_reported takeup columns are not substitutes for required takeup input leaves.
  would_file_taxes_voluntarily:
    entity: tax_unit
    role: takeup_input_contract
    temporary: true
    ecps:
      method: PolicyEngine takeup input/default or eCPS reported-benefit driver
      code:
      - path: policyengine_us_data/datasets/cps/cps.py
        summary: Enhanced CPS benefit/takeup source inputs and defaulted takeup leaves.
    mp_legacy:
      method: deterministic Microplex takeup helper/default
      code:
      - path: src/microplex_us/policyengine/takeup.py
        lines: 14-115,171-253,694-728
        summary: Default takeup rates and feature families.
      - path: src/microplex_us/pipelines/us.py
        lines: 9682-10337
        summary: Legacy pipeline materialization of deterministic takeup inputs.
    mp_spec:
      method: rerandomized_takeup handler or deterministic takeup transform
      operation:
        kind: rerandomize_takeup
        handler: policyengine_us_takeup
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Temporary declaration until takeup transforms are fully serialized.
      notes: Must be explicit because forbidden *_reported takeup columns are not substitutes for required takeup input leaves.
  employment_income:
    entity: person
    role: puf_imputed_non_export
    temporary: true
    ecps:
      method: PUF QRF imputation or calculated PUF support variable
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES
        summary: eCPS PUF imputation/intermediate support surface.
    mp_legacy:
      method: PUF support-clone donor imputation/intermediate surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and temporary variable provenance.
      notes: Synthetic half synthesizes from PUF; CPS keep half may pass through unless explicitly overridden. Not a required
        eCPS export-contract column; retained here because the current spec imputation graph declares it or uses it to feed
        required downstream exports.
  social_security:
    entity: person
    role: puf_imputed_non_export
    temporary: true
    ecps:
      method: PUF QRF imputation or calculated PUF support variable
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES
        summary: eCPS PUF imputation/intermediate support surface.
    mp_legacy:
      method: PUF support-clone donor imputation/intermediate surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      notes: MP legacy keeps CPS values for same-name non-overridden columns; this is explicitly called out for known collisions.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and temporary variable provenance.
      notes: 'Known keep-CPS collision: the synthetic half must synthesize this variable and must not inherit the CPS canonical
        value. Not a required eCPS export-contract column; retained here because the current spec imputation graph declares
        it or uses it to feed required downstream exports.'
  taxable_pension_income:
    entity: person
    role: puf_imputed_non_export
    temporary: true
    ecps:
      method: PUF QRF imputation or calculated PUF support variable
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES
        summary: eCPS PUF imputation/intermediate support surface.
    mp_legacy:
      method: PUF support-clone donor imputation/intermediate surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      notes: MP legacy keeps CPS values for same-name non-overridden columns; this is explicitly called out for known collisions.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and temporary variable provenance.
      notes: 'Known keep-CPS collision: the synthetic half must synthesize this variable and must not inherit the CPS canonical
        value. Not a required eCPS export-contract column; retained here because the current spec imputation graph declares
        it or uses it to feed required downstream exports.'
  interest_deduction:
    entity: person
    role: puf_imputed_overridden_non_export
    temporary: true
    ecps:
      method: PUF QRF imputation or calculated PUF support variable
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES
        summary: eCPS PUF imputation/intermediate support surface.
    mp_legacy:
      method: PUF support-clone donor imputation/intermediate surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables. Not
        a required eCPS export-contract column; retained here because the current spec imputation graph declares it or uses
        it to feed required downstream exports.
  tax_exempt_pension_income:
    entity: person
    role: puf_imputed_non_export
    temporary: true
    ecps:
      method: PUF QRF imputation or calculated PUF support variable
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES
        summary: eCPS PUF imputation/intermediate support surface.
    mp_legacy:
      method: PUF support-clone donor imputation/intermediate surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and temporary variable provenance.
      notes: Synthetic half synthesizes from PUF; CPS keep half may pass through unless explicitly overridden. Not a required
        eCPS export-contract column; retained here because the current spec imputation graph declares it or uses it to feed
        required downstream exports.
  long_term_capital_gains:
    entity: person
    role: puf_imputed_non_export
    temporary: true
    ecps:
      method: PUF QRF imputation or calculated PUF support variable
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES
        summary: eCPS PUF imputation/intermediate support surface.
    mp_legacy:
      method: PUF support-clone donor imputation/intermediate surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and temporary variable provenance.
      notes: Synthetic half synthesizes from PUF; CPS keep half may pass through unless explicitly overridden. Not a required
        eCPS export-contract column; retained here because the current spec imputation graph declares it or uses it to feed
        required downstream exports.
  pre_tax_contributions:
    entity: person
    role: puf_imputed_overridden_non_export
    temporary: true
    ecps:
      method: PUF QRF imputation or calculated PUF support variable
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES
        summary: eCPS PUF imputation/intermediate support surface.
    mp_legacy:
      method: PUF support-clone donor imputation/intermediate surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables. Not
        a required eCPS export-contract column; retained here because the current spec imputation graph declares it or uses
        it to feed required downstream exports.
  self_employment_income:
    entity: person
    role: puf_imputed_non_export
    temporary: true
    ecps:
      method: PUF QRF imputation or calculated PUF support variable
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES
        summary: eCPS PUF imputation/intermediate support surface.
    mp_legacy:
      method: PUF support-clone donor imputation/intermediate surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
      notes: MP legacy keeps CPS values for same-name non-overridden columns; this is explicitly called out for known collisions.
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and temporary variable provenance.
      notes: 'Known keep-CPS collision: the synthetic half must synthesize this variable and must not inherit the CPS canonical
        value. Not a required eCPS export-contract column; retained here because the current spec imputation graph declares
        it or uses it to feed required downstream exports.'
  sstb_self_employment_income:
    entity: person
    role: puf_imputed_overridden_non_export
    temporary: true
    ecps:
      method: PUF QRF imputation or calculated PUF support variable
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES
        summary: eCPS PUF imputation/intermediate support surface.
    mp_legacy:
      method: PUF support-clone donor imputation/intermediate surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables. Not
        a required eCPS export-contract column; retained here because the current spec imputation graph declares it or uses
        it to feed required downstream exports.
  self_employed_pension_contribution_ald:
    entity: person
    role: puf_imputed_overridden_non_export
    temporary: true
    ecps:
      method: PUF QRF imputation or calculated PUF support variable
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES
        summary: eCPS PUF imputation/intermediate support surface.
    mp_legacy:
      method: PUF support-clone donor imputation/intermediate surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables. Not
        a required eCPS export-contract column; retained here because the current spec imputation graph declares it or uses
        it to feed required downstream exports.
  taxable_unemployment_compensation:
    entity: person
    role: puf_imputed_overridden_non_export
    temporary: true
    ecps:
      method: PUF QRF imputation or calculated PUF support variable
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES
        summary: eCPS PUF imputation/intermediate support surface.
    mp_legacy:
      method: PUF support-clone donor imputation/intermediate surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables. Not
        a required eCPS export-contract column; retained here because the current spec imputation graph declares it or uses
        it to feed required downstream exports.
  self_employed_health_insurance_ald:
    entity: person
    role: puf_imputed_overridden_non_export
    temporary: true
    ecps:
      method: PUF QRF imputation or calculated PUF support variable
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES
        summary: eCPS PUF imputation/intermediate support surface.
    mp_legacy:
      method: PUF support-clone donor imputation/intermediate surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables. Not
        a required eCPS export-contract column; retained here because the current spec imputation graph declares it or uses
        it to feed required downstream exports.
  cdcc_relevant_expenses:
    entity: person
    role: puf_imputed_overridden_non_export
    temporary: true
    ecps:
      method: PUF QRF imputation or calculated PUF support variable
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES
        summary: eCPS PUF imputation/intermediate support surface.
    mp_legacy:
      method: PUF support-clone donor imputation/intermediate surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables. Not
        a required eCPS export-contract column; retained here because the current spec imputation graph declares it or uses
        it to feed required downstream exports.
  foreign_tax_credit:
    entity: person
    role: puf_imputed_overridden_non_export
    temporary: true
    ecps:
      method: PUF QRF imputation or calculated PUF support variable
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES
        summary: eCPS PUF imputation/intermediate support surface.
    mp_legacy:
      method: PUF support-clone donor imputation/intermediate surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables. Not
        a required eCPS export-contract column; retained here because the current spec imputation graph declares it or uses
        it to feed required downstream exports.
  american_opportunity_credit:
    entity: person
    role: puf_imputed_overridden_non_export
    temporary: true
    ecps:
      method: PUF QRF imputation or calculated PUF support variable
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES
        summary: eCPS PUF imputation/intermediate support surface.
    mp_legacy:
      method: PUF support-clone donor imputation/intermediate surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables. Not
        a required eCPS export-contract column; retained here because the current spec imputation graph declares it or uses
        it to feed required downstream exports.
  general_business_credit:
    entity: person
    role: puf_imputed_overridden_non_export
    temporary: true
    ecps:
      method: PUF QRF imputation or calculated PUF support variable
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES
        summary: eCPS PUF imputation/intermediate support surface.
    mp_legacy:
      method: PUF support-clone donor imputation/intermediate surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables. Not
        a required eCPS export-contract column; retained here because the current spec imputation graph declares it or uses
        it to feed required downstream exports.
  energy_efficient_home_improvement_credit:
    entity: person
    role: puf_imputed_overridden_non_export
    temporary: true
    ecps:
      method: PUF QRF imputation or calculated PUF support variable
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES
        summary: eCPS PUF imputation/intermediate support surface.
    mp_legacy:
      method: PUF support-clone donor imputation/intermediate surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables. Not
        a required eCPS export-contract column; retained here because the current spec imputation graph declares it or uses
        it to feed required downstream exports.
  amt_foreign_tax_credit:
    entity: person
    role: puf_imputed_overridden_non_export
    temporary: true
    ecps:
      method: PUF QRF imputation or calculated PUF support variable
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES
        summary: eCPS PUF imputation/intermediate support surface.
    mp_legacy:
      method: PUF support-clone donor imputation/intermediate surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables. Not
        a required eCPS export-contract column; retained here because the current spec imputation graph declares it or uses
        it to feed required downstream exports.
  excess_withheld_payroll_tax:
    entity: person
    role: puf_imputed_overridden_non_export
    temporary: true
    ecps:
      method: PUF QRF imputation or calculated PUF support variable
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES
        summary: eCPS PUF imputation/intermediate support surface.
    mp_legacy:
      method: PUF support-clone donor imputation/intermediate surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables. Not
        a required eCPS export-contract column; retained here because the current spec imputation graph declares it or uses
        it to feed required downstream exports.
  savers_credit:
    entity: person
    role: puf_imputed_overridden_non_export
    temporary: true
    ecps:
      method: PUF QRF imputation or calculated PUF support variable
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES
        summary: eCPS PUF imputation/intermediate support surface.
    mp_legacy:
      method: PUF support-clone donor imputation/intermediate surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables. Not
        a required eCPS export-contract column; retained here because the current spec imputation graph declares it or uses
        it to feed required downstream exports.
  early_withdrawal_penalty:
    entity: person
    role: puf_imputed_overridden_non_export
    temporary: true
    ecps:
      method: PUF QRF imputation or calculated PUF support variable
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES
        summary: eCPS PUF imputation/intermediate support surface.
    mp_legacy:
      method: PUF support-clone donor imputation/intermediate surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables. Not
        a required eCPS export-contract column; retained here because the current spec imputation graph declares it or uses
        it to feed required downstream exports.
  prior_year_minimum_tax_credit:
    entity: person
    role: puf_imputed_overridden_non_export
    temporary: true
    ecps:
      method: PUF QRF imputation or calculated PUF support variable
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES
        summary: eCPS PUF imputation/intermediate support surface.
    mp_legacy:
      method: PUF support-clone donor imputation/intermediate surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables. Not
        a required eCPS export-contract column; retained here because the current spec imputation graph declares it or uses
        it to feed required downstream exports.
  other_credits:
    entity: person
    role: puf_imputed_overridden_non_export
    temporary: true
    ecps:
      method: PUF QRF imputation or calculated PUF support variable
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES
        summary: eCPS PUF imputation/intermediate support surface.
    mp_legacy:
      method: PUF support-clone donor imputation/intermediate surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables. Not
        a required eCPS export-contract column; retained here because the current spec imputation graph declares it or uses
        it to feed required downstream exports.
  unreported_payroll_tax:
    entity: person
    role: puf_imputed_overridden_non_export
    temporary: true
    ecps:
      method: PUF QRF imputation or calculated PUF support variable
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES
        summary: eCPS PUF imputation/intermediate support surface.
    mp_legacy:
      method: PUF support-clone donor imputation/intermediate surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables. Not
        a required eCPS export-contract column; retained here because the current spec imputation graph declares it or uses
        it to feed required downstream exports.
  recapture_of_investment_credit:
    entity: person
    role: puf_imputed_overridden_non_export
    temporary: true
    ecps:
      method: PUF QRF imputation or calculated PUF support variable
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES
        summary: eCPS PUF imputation/intermediate support surface.
    mp_legacy:
      method: PUF support-clone donor imputation/intermediate surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables. Not
        a required eCPS export-contract column; retained here because the current spec imputation graph declares it or uses
        it to feed required downstream exports.
  deductible_mortgage_interest:
    entity: person
    role: puf_imputed_overridden_non_export
    temporary: true
    ecps:
      method: PUF QRF imputation or calculated PUF support variable
      code:
      - path: policyengine_us_data/storage/enhanced_cps/puf_impute.py
        lines: 36-153,227,243-248
        symbol: IMPUTED_VARIABLES / OVERRIDDEN_IMPUTED_VARIABLES
        summary: eCPS PUF imputation/intermediate support surface.
    mp_legacy:
      method: PUF support-clone donor imputation/intermediate surface
      code:
      - path: src/microplex_us/pipelines/us.py
        lines: 182-258,326-387,2035-2061,6088-6683
        symbol: PUF_SUPPORT_CLONE_IMPUTED_VARIABLES / PUF_SUPPORT_CLONE_OVERRIDDEN_VARIABLES / PUF_SUPPORT_CLONE_SPECIAL_VARIABLES
    mp_spec:
      method: declared PUF imputation step
      operation:
        kind: impute
        source: puf
        imputation_step: puf_support_clone
      code:
      - path: src/microplex_us/specs/us-2024.yaml
        summary: Listed in imputation.vars and temporary variable provenance.
      notes: Synthetic half synthesizes from PUF. CPS keep half is overwritten/collapsed for eCPS OVERRIDDEN variables. Not
        a required eCPS export-contract column; retained here because the current spec imputation graph declares it or uses
        it to feed required downstream exports.

targets:
  arch:
    country: us
    model_year: 2024
    manifest: manifests/arch_targets.json
    target_profile: pe_native_broad
    calibration_target_profile: pe_native_broad_source_backed

calibrate:
  loss: pe_native_bucketed_huber_v1
  method: apg
